<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <!--
        An empty FrameLayout that can be inflated and returned by Fragment#onCreateView(...) when the
        views are added as children of the <fragment> tag in the Activity layout.
    -->
</FrameLayout>
